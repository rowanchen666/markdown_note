# 说明

位运算符包括：&（按位与  AND） 、|（按位或  OR）、^（按位异或  XOR） 、<<（左移）、>>（右移）、~（按位取反） 。<font color = 'orange'>只有~为一元运算符</font>，其余都为二元运算符。这些运算符只能作用于整数，即包括short、long、int、<font color = 'orange'>char</font>（无论是否带符号）

# &  按位与  （AND）

遵循<font color = 'orange'>“与”</font>的原则，用途如下
#### 1. **掩码操作（Masking）**

最常见的用途是提取特定位或清除某些位：

```c++
// 提取低8位
int value = 0xABCD;
int lowByte = value & 0xFF;  // 0xCD

// 检查特定位是否设置
int flags = 0b10101100;
if (flags & 0b00001000) {  // 检查第3位
    // 位已设置
}

// 清除某些位
int data = 0xFF;
int cleared = data & ~0x0F;  // 清除低4位
```

技巧是只把想要留下的位写为1，其余为0，并放在后面作为右操作数。

#### 2. **奇偶判断**

```c++
bool isEven(int n) {
    return (n & 1) == 0;  // 最后一位为0则是偶数
}

bool isPowerOfTwo(int n) {
    return n > 0 && (n & (n - 1)) == 0;
}
```


# |  按位或  （OR）

#### **设置位（设置标志）**

最常见用途是设置特定位为1：

```c++
// 设置权限标志
enum Flags {
    NONE = 0,
    READ = 1 << 0,   // 0001
    WRITE = 1 << 1,  // 0010
    EXECUTE = 1 << 2 // 0100
};

int permissions = NONE;
permissions |= READ;      // 添加读权限：0001
permissions |= WRITE;     // 添加写权限：0011
permissions |= EXECUTE;   // 添加执行权限：0111
```

# ^ 按位异或 （XOR）

#### 1. **切换/翻转位（Toggle）**

最常见的用途是切换位的状态：

```c++
// 切换特定位
int flags = 0b1010;
flags ^= 0b0101;  // 结果是 0b1111

// 切换单个位
int toggleBit(int value, int bitPosition) {
    return value ^ (1 << bitPosition);
}

// 示例：切换第3位（从0开始）
int num = 0b1101;  // 13
num = num ^ (1 << 3);  // 切换第3位：0b0101 (5)
```

#### 2.  **交换两个变量的值（不使用临时变量）**

```c++
// XOR交换算法
void swap(int& a, int& b) {
    a = a ^ b;
    b = a ^ b;  // 现在 b = 原来的 a
    a = a ^ b;  // 现在 a = 原来的 b
}

// 示例
int x = 5, y = 9;
swap(x, y);  // x=9, y=5
```

# <<  左移     >>  右移

#### **乘以/除以2的幂（高效运算）**

```c++
// 乘以2^n
int multiplyByPowerOfTwo(int x, int n) {
    return x << n;  // x * (2^n)
}

// 除以2^n（对正数和无符号数）
int divideByPowerOfTwo(int x, int n) {
    return x >> n;  // x / (2^n)
}

// 示例
int a = 5;
int b = a << 2;  // 5 * 4 = 20
int c = a >> 1;  // 5 / 2 = 2（向下取整）
```

[注意]：两者的右运算符<font color = 'orange'>必须为正数</font>

# ~  按位取反

#### **创建掩码和位清除**

最常见的用途是创建位掩码：

```c++
// 清除低4位
int value = 0xFF;
int cleared = value & ~0x0F;  // 0xF0

// 清除特定位
int flags = 0b1011;
int mask = 0b0100;  // 要清除第2位
flags = flags & ~mask;  // 0b0011

// 通用的位清除函数
int clearBits(int value, int bitMask) {
    return value & ~bitMask;
}
```

# 用法与技巧（针对连续位置的操作）

#### 1. 构造特定的工具
对`~0`进行左移与右移，得到特定位置全为1，其余为0的操作数；若在按位取反，则得到特定位置全为0，其余为1的操作数。

#### 2. 保留（保留特定位置，其余删除）
1. 特定位置取1，其余为0，用`&`
2. 特定位置取0，其余为1，用`|`

#### 3. 删除（删除特定位置，其余保留）
1. 特定位置取1，其余为0，用`|`
2. 特定位置取0，其余为1，用`&`

#### 4. 覆盖
1. 特定位置原本全为0，用`|`
2. 特定位置原本全为1，用`&`

[总结]  操作顺序为：构造工具——对原数字进行保留或删除特定位置——覆盖特定位置