1. **字符常量与字符串**
	`'x'`与`"x"`是不同的，前者是一个整数，其值是字母x在机器字符集中对应的数值（例如ASCII）；后者是包含一个字符x以及<font color="red">一个结束符'\0'</font>的<font color="red">字符数组</font>

2. **枚举类型**
	在C++中，枚举类型有两种主要写法：传统的C风格枚举和C++11引入的强类型枚举。
	1. 传统的C风格枚举
```c++
// 基本语法
enum Color {
    RED,      // 默认值为0
    GREEN,    // 默认值为1
    BLUE      // 默认值为2
};

// 可以指定值
enum Weekday {
    MONDAY = 1,
    TUESDAY,    // 自动为2
    WEDNESDAY,  // 自动为3
    THURSDAY = 8,
    FRIDAY      // 自动为9
};

// 使用示例
Color c = RED;
if (c == GREEN) {
    // ...
}
```
	
	1. C++11引入的强类型枚举