```c++
#include <iostream>
#include <cstdlib>
#include <ctime>
int main() {
    srand(time(0));
    int num = rand() % 100 + 1;  // 1~100的随机整数
    std::cout << num;
    return 0;
}
```

[注意]：`rand() % a`表示生成0到<font color ='red'>(a - 1)</font>的随机整数，`rand () % a + b`表示生成b到<font color ='red'>(a + b - 1)</font>的随机整数。